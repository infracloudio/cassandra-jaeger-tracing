version: 2
jobs:
build:
  working_directory: /tmp
  steps
    - run:
        name: Compiling
        command: |
          mvn package
          mkdir /tmp/artifacts;
    - store_artifacts:
        path: /tmp/art-1
        destination: artifact-file
        echo cassandra-jaeger* /tmp/artifact-file
      - store_artifacts:
        path: /tmp/artifacts